import {
  FormHelperText as ChakraFormHelperText,
  HelpTextProps,
} from '@chakra-ui/react'

export default function FormHelperText(props: HelpTextProps) {
  return <ChakraFormHelperText {...props} />
}
